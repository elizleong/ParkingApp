var d = new Date();
var curr_hour = d.getHours();
var curr_min = d.getMinutes();


	if (curr_hour + ":" + curr_min == "13:45" ){
		alert("You can park for free!")
		}
	else{
		alert("You can't park")
		}
